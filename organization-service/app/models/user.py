"""User model placeholder.

Users are managed by the auth service. This module exists for schema
consistency across the service layer -- the organization service calls
the auth service for all user CRUD and does not store user rows locally.
"""
